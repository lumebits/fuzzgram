name: template_repository
version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^1.2.5
  cloud_firestore: ^0.16.0
  firebase_core: ^0.7.0
  rxdart: ^0.25.0
